// app.js

/*

                if(x!=0 || x!=numArr.length){
                    if(numArr[x]==randomArr[x-1]) { result[x] = 'L'; }
                    if(numArr[x]==randomArr[x+1]) { result[x] = 'R'; }

                }
                else if(x==0){
                    if(numArr[x]==randomArr[numArr.length]) { result[x] = 'L'; }
                    if(numArr[x]==randomArr[x+1]) { result[x] = 'R'; }

                }
                else if(x==numArr.length){
                    if(numArr[x]==randomArr[0]) { result[x] = 'L'; }
                    if(numArr[x]==randomArr[x-1]) { result[x] = 'R'; }

                }

*/
